name: Docker Build

on:
  pull_request:
    branches: [ develop ]

jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build app image
        run: docker build -t sis-app-ci -f Dockerfile .
